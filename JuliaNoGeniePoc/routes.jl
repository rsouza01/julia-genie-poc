# using Genie.Router, Genie.Renderer.Json, Genie.Responses, Genie.Requests
using JSON
using UUIDs
using Dates

# route("/") do
#   serve_static_file("welcome.html")
# end

# route("/live", method = GET) do
#   setstatus(200)
#   setheaders(Dict("Content-Type"=>"application/json; charset=utf-8"))
#   setbody(JSON.json("Ok"))
#   getresponse()
# end
